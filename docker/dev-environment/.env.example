# Dev Environment Configuration
# Copy to .env and customize

# =============================================================================
# Network - macvlan on br0
# =============================================================================
DEV_IP=192.168.20.60
DEV_SUBNET=192.168.20.0/24
DEV_GATEWAY=192.168.20.1
DEV_INTERFACE=br0

# =============================================================================
# Container user
# =============================================================================
DEV_USER=james

# =============================================================================
# Home mount safety controls
# =============================================================================
# true: do not chown/create files in the mounted home directory during startup
DEV_SAFE_MODE=true
# true: allow container startup to manage .zshrc/.env_container/.bashrc in home
DEV_MANAGE_HOME_CONFIGS=false

# Optional Compose profiles (for example: tailscale)
DEV_COMPOSE_PROFILES=

# =============================================================================
# Resource limits
# =============================================================================
DEV_MEMORY_LIMIT=8g
DEV_CPU_LIMIT=4.0

# =============================================================================
# Storage paths (Unraid appdata)
# =============================================================================
APPDATA_PATH=/mnt/user/appdata/dev-environment

# =============================================================================
# 1Password SSH agent socket (path on host)
# =============================================================================
OP_SSH_AGENT_SOCK=/home/james/.1password/agent.sock

# =============================================================================
# VS Code Remote Tunnel
# =============================================================================
VSCODE_TUNNEL_NAME=dev-box

# =============================================================================
# Tailscale sidecar (enable with DEV_COMPOSE_PROFILES=tailscale)
# =============================================================================
TAILSCALE_AUTHKEY=
TAILSCALE_HOSTNAME=dev-box
TAILSCALE_ACCEPT_DNS=false
TAILSCALE_EXTRA_ARGS=--ssh

# =============================================================================
# API Keys
# =============================================================================
OPENROUTER_API_KEY=
LMSTUDIO_API_KEY=
OP_SERVICE_ACCOUNT_TOKEN=
GITEA_TOKEN=
LMSTUDIO_BASE_URL=http://spraycheese.lab.klsll.com:1234/v1

# =============================================================================
# Timezone
# =============================================================================
TZ=America/Phoenix
