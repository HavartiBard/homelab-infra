# TODO: no 1Password item exists yet for NetBox's DB password, Django
# SECRET_KEY, or API token (a token is a distinct value from the superuser's
# login password, generated separately) — create dedicated items (or add
# fields to the existing "Netbox" item) before deploying/using these, then
# fill in these three lines. Confirmed with the user this gap is expected for
# now, not silently worked around.
# NETBOX_DB_PASSWORD=op://AI Wedge/<TODO>/<TODO>
# NETBOX_SECRET_KEY=op://AI Wedge/<TODO>/<TODO>
NETBOX_SUPERUSER_PASSWORD=op://AI Wedge/Netbox/password
NETBOX_TOKEN=op://AI Wedge/Netbox AI Token/password
