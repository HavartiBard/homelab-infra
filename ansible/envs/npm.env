NPM_ADMIN_EMAIL=op://AI Wedge/Nginx Proxy Manager Admin/username
NPM_ADMIN_PASSWORD=op://AI Wedge/Nginx Proxy Manager Admin/password
TECHNITIUM_API_TOKEN=op://AI Wedge/Technitium - AI agent/password
# Fallback login only, used when TECHNITIUM_API_TOKEN is empty (see
# roles/npm/tasks/proxies.yml). Was op://AI Wedge/Technitium Admin/password
# — both that item and the old "DNS Automation Credential" API token were
# found invalid against the live server on 2026-07-21 (confirmed via direct
# API calls, not just assumed stale); replaced with a freshly-generated
# token/account ("Technitium - AI agent") rather than chasing why the old
# ones broke.
TECHNITIUM_ADMIN_PASSWORD=op://AI Wedge/Technitium - AI agent/password
CLOUDFLARE_DNS_TOKEN=op://AI Wedge/Cloudflare DNS Token/credential
