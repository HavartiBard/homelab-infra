# Cross-cutting secrets merged into every run-playbook.sh invocation
# (regardless of slug) — for values needed by roles that many other
# roles/playbooks include, like netbox-service's registration token.
NETBOX_TOKEN=op://AI Wedge/Netbox AI Token/password
