# NOTE: the item's title is "Service Account Auth Token: lyra" (AI Wedge vault),
# but its literal colon breaks op:// secret-reference parsing (confirmed:
# `op read`/`op run` both fail with "invalid character in secret reference: ':'"
# and "could not read secret" regardless of escaping/quoting attempted).
# Referencing by the item's stable ID (nnyb43blqhjzx7vzrxdjtqtsvy) instead —
# same item, verified via `op item get "Service Account Auth Token: lyra" --vault "AI Wedge"`.
# If this item is ever deleted and recreated (not just edited/renamed), its ID
# changes and this reference must be updated — re-run `op item get` by title
# to find the new ID.
OP_SERVICE_ACCOUNT_TOKEN_LYRA=op://AI Wedge/nnyb43blqhjzx7vzrxdjtqtsvy/credential
