---
proxmox_mcp_image: "ghcr.io/havartibard/proxmox-mcp:latest"
proxmox_mcp_container_name: "proxmox-mcp"
proxmox_mcp_icon: >-
  https://cdn.jsdelivr.net/gh/homarr-labs/dashboard-icons/png/proxmox.png
proxmox_mcp_webui_url: >-
  http://{{ ansible_host }}:{{ proxmox_mcp_port }}/mcp

proxmox_mcp_appdata_dir: "/mnt/user/appdata/proxmox-mcp"
proxmox_mcp_env_file: "{{ proxmox_mcp_appdata_dir }}/.env"
# Host port for the MCP service (container listens on proxmox_mcp_container_port)
proxmox_mcp_port: "6974"
proxmox_mcp_container_port: "6971"

proxmox_host: "{{ lookup('ansible.builtin.env', 'PROXMOX_HOST') | default('', true) }}"
proxmox_port: "{{ lookup('ansible.builtin.env', 'PROXMOX_PORT') | default('', true) }}"
proxmox_user: "{{ lookup('ansible.builtin.env', 'PROXMOX_USER') | default('', true) }}"
proxmox_token_name: "{{ lookup('ansible.builtin.env', 'PROXMOX_TOKEN_NAME') | default('', true) }}"
proxmox_token_value: "{{ lookup('ansible.builtin.env', 'PROXMOX_TOKEN_VALUE') | default('', true) }}"
proxmox_allow_elevated: "{{ lookup('ansible.builtin.env', 'PROXMOX_ALLOW_ELEVATED') | default('', true) }}"

# Timezone
timezone: "America/Phoenix"
